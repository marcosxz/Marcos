algoritmo "VerificarNotaAprovacao"
// Função : Verificar se a nota é suficiente para aprovação
var
    nota: real

inicio
    escreva("Digite a nota: ")
    leia(nota)

    se (nota >= 7) então
        escreva("Nota suficiente para aprovação.")
    fimse
fimalgoritmo


